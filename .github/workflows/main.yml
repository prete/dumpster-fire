name: 🐻

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:

  test:  
    name: 🐕
    
    runs-on: ubuntu-latest
    container:
      image: quay.io/cellgeni/scrna-seq-course:v5.11
      options: --user root

    steps:
      - name: Free workspace storage
        run: df -h
      - name: Delete stuff
        run: sudo rm -rf /usr/shared/dotnet
      - name: Storage after delete
        run: df -h
